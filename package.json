{
  "name": "auctionoton",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "ni:e": "npm install -w auctionoton-extension",
    "ni:s": "npm install -w auctionoton-server",
    "nu:e": "npm uninstall -w auctionoton-extension",
    "nu:s": "npm uninstall -w auctionoton-server",
    "cs:add": "npx changeset",
    "cs:ver": "npx changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "2.19.0",
    "turbo": "latest"
  },
  "turbo": {
    "pipeline": {
      "build": {
        "dependsOn": [
          "^build"
        ],
        "outputs": [
          "dist/**",
          ".next/**"
        ]
      },
      "lint": {
        "outputs": []
      },
      "dev": {
        "cache": false
      }
    }
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  }
}
